server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/servicehub_auth
    username: postgres
    password: Divya@88

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-u4hrzsmmp2bxfd6a.us.auth0.com/
  jpa:
     hibernate:
       ddl-auto: validate
       show-sql: false # Production లో logs పెరగకుండా ఉండటానికి false పెట్టడం మంచిది

  flyway:
    enabled: true

management:
  endpoints:
    web:
     exposure:
       include: "health,metrics,prometheus" # కేవలం అవసరమైనవి మాత్రమే Expose చేయాలి
#  endpoint:
#    health:
#      show-details: when_authorized # Health details కూడా అందరికీ చూపించకుండా సెక్యూర్ చేయడం

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

  